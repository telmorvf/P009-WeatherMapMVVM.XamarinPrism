<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:busyindicator="clr-namespace:Syncfusion.SfBusyIndicator.XForms;assembly=Syncfusion.SfBusyIndicator.XForms"
    xmlns:prism="http://prismlibrary.com"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
    xmlns:ffimageloadingSvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    xmlns:i18n="clr-namespace:WeatherMapMVVM.Helpers"
    prism:ViewModelLocator.AutowireViewModel="True"
    Title="{Binding Title, Title}"
    BackgroundColor="{StaticResource ColorBackground}"
    x:Class="WeatherMapMVVM.Views.ForecastPage">
    
    <AbsoluteLayout>

        <StackLayout
            Padding="5"
            AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="All">

            <SearchBar
                Placeholder="{i18n:Translate SearchCity}"
                SearchCommand="{Binding SearchCommand}"
                Text="{Binding Search}" />

            <CollectionView           
                ItemsSource="{Binding ObseForecast}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout
                        Orientation="Vertical" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>

                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding SelectCityCommand}" />
                            </Grid.GestureRecognizers>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition
                                    Width="Auto" />
                                <ColumnDefinition
                                    Width="*" />
                                <ColumnDefinition
                                    Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ffimageloadingSvg:SvgCachedImage
                                Grid.Column="0"
                                Margin="5"
                                Aspect="AspectFill"
                                CacheDuration="50"
                                RetryCount="3"
                                RetryDelay="600"
                                Source="{Binding WTypeImage}"
                                WidthRequest="100" />
                            <StackLayout
                                Grid.Column="1"
                                VerticalOptions="Center">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="Medium"
                                    TextColor="Black"
                                    LineBreakMode="TailTruncation"
                                    Text="{Binding Local}" />

                                <Label
                                    FontAttributes="Bold, Italic" 
                                    VerticalOptions="End">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span
                                                Text="{Binding WType}" />

                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Label
                                    
                                    FontAttributes="Bold, Italic"
                                    VerticalOptions="End">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span
                                                Text="{Binding TMax, StringFormat='Max.: {0} ºC   '}" />
                                            <Span
                                                Text="{Binding TMin, StringFormat='    Min.: {0} ºC'}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </StackLayout>
                            <Image
                                Grid.Column="2"
                                Source="ic_more_vert" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>


        <busyindicator:SfBusyIndicator
            Title="{i18n:Translate Loading}"
            AbsoluteLayout.LayoutBounds=".5,.5,.5,.5"
            AbsoluteLayout.LayoutFlags="All"
            AnimationType="Cupertino"
            BackgroundColor="{StaticResource ColorBackground}"
            HorizontalOptions="Center"
            IsBusy="{Binding IsRunning}"
            TextColor="{StaticResource ColorFont}"
            VerticalOptions="Center"
            ViewBoxHeight="120"
            ViewBoxWidth="120" />

    </AbsoluteLayout>

</ContentPage>